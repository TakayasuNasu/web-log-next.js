# AGENTS.md

## Repository expectations

- Run `bun run lint` before opening a pull request.
- Document public utilities in `docs/` when you change behavior.
- After any file edit or addition, inspect `git diff` and output a one-line commit message suggestion.
- Keep the commit message to a single line only, with no explanation or extra formatting.
